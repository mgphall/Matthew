<views:MvxWpfView 
             x:Class="PrimeTables.WPF.Views.PrimeTablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <StackPanel Height="40" Orientation="Horizontal" VerticalAlignment="Top">
            <Controls:NumericUpDown Width="200" Value="{Binding PrimeCount}" Minimum = "0" Maximum = "10000" />
            <Button Content="Start"  Style="{StaticResource AccentedSquareButtonStyle}"  Command="{Binding StartPrimeCalulation}"/>
        </StackPanel>

        <StackPanel>
            <!--<DataGrid x:Name="grid"  Background="Red"
                   ItemsSource="{Binding DataView}"
                  AutoGenerateColumns="True" />-->

        </StackPanel>
    </StackPanel>
</views:MvxWpfView>
